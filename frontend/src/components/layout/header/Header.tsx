import { MainHeader } from './main-header/MainHeader';
import { TopBar } from './Top-bar/TopBar';
;

const Header = () => {
  return (
    <div>
      <TopBar />
      <MainHeader />
    </div>
  );
};

export default Header;
